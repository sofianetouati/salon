{% extends 'admin/base.html.twig' %}

{% block title %}Ajouter un Rendez-vous{% endblock %}
{% block body %}


    <div id="page-container" class="sidebar-o sidebar-dark enable-page-overlay side-scroll page-header-fixed main-content-narrow page-header-dark">
    {% include "admin/partials/sidebar.html.twig" %}
    {% include "admin/partials/header.html.twig" %}
    {# {% include "admin/partials/options.html.twig" %}  #}

      <!-- Main Container -->
      <main id="main-container">

        <!-- Hero -->
        <div class="bg-image" style="background-image: url('{{ asset('build/images/photos/photo13@2x.jpg') }}');">
          <div class="bg-black-50">
            <div class="content content-full">
              <div class="d-flex justify-content-between align-items-center">
                <h1 class="fs-2 text-white my-2">
                  <i class="fa fa-plus text-white-50 me-1"></i> Ajouter un rendez-vous
                </h1>
                <div>
                  <a class="btn btn-primary my-2 text-light" href="{{ path('admin_booking_all') }}">
                      <i class="fa fa-fw fa-list"></i>
                      <span class="d-none d-sm-inline ms-1">Liste des réservations</span>
                  </a>
                  <a class="btn btn-primary my-2 text-light" href="{{ app.request.headers.get('referer') }}">
                    <i class="fa fa-fw fa-undo"></i>
                    <spane class="d-none d-sm-inline ms-1">Retour</spane>
                  </a>
                </div>
              </div>  
            </div>
          </div>
        </div>
        <!-- END Hero -->

        <!-- Page Content -->
        <div class="content">
          <div class="row items-push">
          {% if salon is iterable %}
            
          
          {% for s in salon %}
            <div class="col-md-6 col-xl-4">
              <!-- Project #1 -->
              <div class="block block-rounded h-100 mb-0">
                {# <div class="block-header">
                  <div class="flex-grow-1 text-muted fs-sm fw-semibold">
                    <i class="fa fa-clock me-1"></i> ajouté le {{ s.createdAt|date("d/m/Y") }}
                  </div>
                  <div class="block-options">
                    <div class="dropdown">
                      <button type="button" class="btn-block-option" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fa fa-ellipsis-v"></i>
                      </button>
                      <div class="dropdown-menu dropdown-menu-end">
                        <a class="dropdown-item" href="{{ path('admin_s_edit', {id:s.id}) }}">
                          <i class="fa fa-fw fa-edit me-1"></i> Modifier
                        </a>
                        <div role="separator" style="color:red;" class="dropdown-divider"></div>
                        <form method="post" class="text-center" action="{{ path('admin_s_delete', {'id': s.id}) }}" onsubmit="return confirm('Voulez-vous vraiement supprimer cet établissement?');">
                          <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ s.id) }}">
                          <button class="btn btn-danger"><i class="fa fa-trash-alt me-1"></i>Supprimer</button>
                        </form>
                      </div>
                    </div>
                  </div>
                </div> #}
                <div class="block-content bg-body-light text-center">
                  <h3 class="fs-4 fw-bold mb-1">
                    <a href="{{ path('admin_booking_new', {id:s.id}) }}">{{ s.name | title }}</a>
                  </h3>
                  <h4 class="fs-6 text-muted mb-3">{{ s.address | title}}</h4>
                  <div class="push">
                    <span class="badge bg-success fw-bold py-2 px-3">{{ s.manager | upper }}</span>
                  </div>
                  <div class="push">
                    <i class="fa fa-fw fa-phone-alt me-1"></i><a href="tel:{{ s.phone }}"><span class="py-2 px-3">{{ s.phone }}</span></a>
                  </div>
                </div>
                <div class="block-content text-center">

                  <i class="fa fa-fw fa-users me-1"></i><span class="py-2 px-3">{{s.coiffeurs|length}} coiffeur{% if s.coiffeurs|length>1 %}s{% endif %}
                    </span>

                </div>
                <div class="block-content block-content-full">
                  <div class="row g-sm">
                    <div class="col-12">
                      <a class="btn w-100 btn-alt-secondary" href="{{ path('admin_booking_new', {id:s.id}) }}">
                        <i class="fa fa-eye me-1 opacity-50"></i> Réserver
                      </a>
                    </div>
                    {# <div class="col-6">
                      <a class="btn w-100 btn-alt-secondary" href="{{ path('admin_calendar') }}">
                        <i class="fa fa-calendar-alt me-1 opacity-50"></i> Calendrier
                      </a>
                    </div> #}
                  </div>
                </div>
              </div>
              <!-- END Project #1 -->
            </div>
            {% endfor %}
            {% else %}
            <div class="col-md-6 col-xl-4">
              <!-- Project #1 -->
              <div class="block block-rounded h-100 mb-0">
                {# <div class="block-header">
                  <div class="flex-grow-1 text-muted fs-sm fw-semibold">
                    <i class="fa fa-clock me-1"></i> ajouté le {{ salon.createdAt|date("d/m/Y") }}
                  </div>
                  <div class="block-options">
                    <div class="dropdown">
                      <button type="button" class="btn-block-option" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fa fa-ellipsis-v"></i>
                      </button>
                      <div class="dropdown-menu dropdown-menu-end">
                        <a class="dropdown-item" href="{{ path('admin_salon_edit', {id:salon.id}) }}">
                          <i class="fa fa-fw fa-edit me-1"></i> Modifier
                        </a>
                        <div role="separator" style="color:red;" class="dropdown-divider"></div>
                        <form method="post" class="text-center" action="{{ path('admin_salon_delete', {'id': salon.id}) }}" onsubmit="return confirm('Voulez-vous vraiement supprimer cet établissement?');">
                          <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salon.id) }}">
                          <button class="btn btn-danger"><i class="fa fa-trash-alt me-1"></i>Supprimer</button>
                        </form>
                      </div>
                    </div>
                  </div>
                </div> #}
                <div class="block-content bg-body-light text-center">
                  <h3 class="fs-4 fw-bold mb-1">
                    <a href="{{ path('admin_booking_new', {id:salon.id}) }}">{{ salon.name | title }}</a>
                  </h3>
                  <h4 class="fs-6 text-muted mb-3">{{ salon.address | title}}</h4>
                  <div class="push">
                    <span class="badge bg-success fw-bold py-2 px-3">{{ salon.manager | upper }}</span>
                  </div>
                  <div class="push">
                    <i class="fa fa-fw fa-phone-alt me-1"></i><a href="tel:{{ salon.phone }}"><span class="py-2 px-3">{{ salon.phone }}</span></a>
                  </div>
                </div>
                <div class="block-content text-center">

                  <i class="fa fa-fw fa-users me-1"></i><span class="py-2 px-3">{{salon.coiffeurs|length}} coiffeur{% if salon.coiffeurs|length>1 %}s{% endif %}
                    </span>

                </div>
                <div class="block-content block-content-full">
                  <div class="row g-sm">
                    <div class="col-12">
                      <a class="btn w-100 btn-alt-secondary" href="{{ path('admin_booking_new', {id:salon.id}) }}">
                        <i class="fa fa-eye me-1 opacity-50"></i> Réserver
                      </a>
                    </div>
                    {# <div class="col-6">
                      <a class="btn w-100 btn-alt-secondary" href="{{ path('admin_calendar') }}">
                        <i class="fa fa-calendar-alt me-1 opacity-50"></i> Calendrier
                      </a>
                    </div> #}
                  </div>
                </div>
              </div>
              <!-- END Project #1 -->
            </div>
          {% endif %}
          {% if salon | length == 0 %}
            <p>Aucun salon n'est pour le moment disponible.</p>
          {% endif %}
          </div>
        </div>
        <!-- END Page Content -->
      </main>
      <!-- END Main Container -->
      {% include "admin/partials/footer.html.twig" %}
    </div>
    <!-- END Page Container -->
    
{% endblock %}