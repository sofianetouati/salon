<form method="post" action="{{ path('admin_coiffeur_delete', {'id': coiffeur.id}) }}" onsubmit="return confirm('Voulez-vous vraiement supprimer ce coiffeur de votre liste?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coiffeur.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash-alt me-1"></i>Supprimer</button>
</form>
