{% extends 'admin/base.html.twig' %}

{% block title %}RestReason{% endblock %}

{% block body %}
    <h1>RestReason</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rest_reason.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ rest_reason.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rest_reason_index') }}">back to list</a>

    <a href="{{ path('rest_reason_edit', {'id': rest_reason.id}) }}">edit</a>

    {{ include('admin/coiffeur/reason/_delete_form.html.twig') }}
{% endblock %}
